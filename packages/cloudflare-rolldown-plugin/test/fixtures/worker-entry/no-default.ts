// A module with named exports but no default export — e.g. React Router's
// `virtual:react-router/server-build` build manifest.
export const routes = { root: { id: "root" } };
export const assets = {};
export const entry = { module: {} };
