// Placeholder so the shared/ bucket has at least one input for tsc.
// @cloudflare/workflows-shared has no isomorphic (shared) surface; everything
// is Workers-runtime code under src/workers/.
export {};
